program factorial

var n: int;

function fact(n: int): int {
    if n <= 0 then
        return 1;
    return n * fact(n - 1)
}

{
    write "enter a number: ";
    read n;
    write n;
    write "! = ";
    write fact(n);
    write "\n"
}
